# pixreveal — Mobile App

Flutter projesi henüz oluşturulmadı.

Oluşturmak için:
```bash
cd mobile
flutter create --org com.cytbilisim . 
```

Veya Claude Code ile:
```
claude "/build Flutter projesi oluştur: pixreveal"
```
